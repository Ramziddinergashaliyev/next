/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "assets.dummyjson.com",
      "cdn.dummyjson.com",
      "encrypted-tbn0.gstatic.com",
    ],
  },
};

export default nextConfig;
